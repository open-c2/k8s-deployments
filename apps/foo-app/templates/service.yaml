apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "foo-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
